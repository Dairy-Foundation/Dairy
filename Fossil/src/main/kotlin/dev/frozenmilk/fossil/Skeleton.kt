package dev.frozenmilk.dairy.archaeological

/**
 * data structure
 */
class Skeleton {
	internal val enum: MutableSet<Enum<*>> = mutableSetOf()
	internal val map: MutableMap<String, Any> = mutableMapOf()
}